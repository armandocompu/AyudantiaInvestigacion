<%- include ('partials/_header') %>
<div class="container mt-5">
  <div class="input-group">
    <form action="/tiposactivo/search" method="GET">
      <div class="input-group">
        <input type="text" name="search" placeholder="Search descripcion" class="form-control">
      <button type="submit" class="btn btn-primary">Search</button>
      </div>
    </form>
  </div>
    <div class="row">
      <div class="col-md-7">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
              <td>nº</td>
              <td>id</td>
              <td>tipo</td>
              <td>descripcion</td>
              <td>Actions</td>
            </tr>
          </thead>
          <tbody>
            <% if (data) { %>
              <% for(var i = 0; i < data.length; i++) { %>
                <tr>
                  <td><%= (i + 1) %></td>
                  <td><%= data[i].id %></td>
                  <td><%= data[i].tipo %></td>
                  <td><%= data[i].descripcion %></td>
                  <td>
                    <a href="tiposactivo/update/<%= data[i].id %>" class="btn btn-info">Edit</a>
                    <a href="tiposactivo/delete/<%= data[i].id %>" class="btn btn-danger">Delete</a>
                  </td>
                </tr>
              <% } %>
            <% } %>
          </tbody>
        </table>
      </div>
      <div class="col-md-5">
        <div class="card-body">
  
          <form action="tiposactivo/add" method="POST">
            <div class="form-group">
              <input type="text" name="tipo" placeholder="Tipo" class="form-control" autofocus>
            </div>
            <div class="form-group">
              <input type="text" name="descripcion" placeholder="Descripcion" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">
              save TipoActivo
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

<%- include ('partials/_footer') %>